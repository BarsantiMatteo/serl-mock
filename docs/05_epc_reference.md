# EPC reference: quirks and region differences (England & Wales vs Scotland)

This reference lists every EPC variable documented in
[`data/reference/edition08/serl_epc_data_dictionary_edition07.csv`](../data/reference/edition08/serl_epc_data_dictionary_edition07.csv)
(the real SERL edition07 schema, 103 variables) and states whether it is available for
England & Wales, for Scotland, or both — plus whether the mock pipeline currently
generates it (`src/serl_mock/generator_contextual_data.py`, `_epc_fields()` /
`generate_epc()`).

Note there are two separate files per edition: the dictionary above is SERL's own real
documentation (kept for cross-reference, per-value vocab, and the `CountriesAvailable` split
used in this doc), while `data/reference/edition<N>/serl_epc_generated_fields.csv` is this
project's own record of which of those 103 fields the mock generator actually implements (81
of them today — the "Not yet generated" rows below). Adding a field means adding it to the
generated-fields list *and* writing its value-generation logic in `generate_epc()`; the
dictionary alone doesn't drive value generation yet.

## Why this matters

The real SERL EPC dataset merges two different national EPC formats. Per the SERL EPC
technical documentation (`docs/documentation/SERL/8666_serl_epc_documentation_edition07.pdf`,
p.1):

> "There are some differences between the English and Welsh EPCs versus the Scottish
> EPCs, both in terms of the variables available and the structure of the data. In
> particular, for the Scottish data if there are multiple types of an element in a
> dwelling then these are recorded in the relevant column and separated by '|'
> [ASCII (decimal) 124], whereas only one type is recorded per element for England
> and Wales."

This produces three distinct patterns, each represented below:

1. **Region-exclusive columns** — 22 variables only exist for Scotland (e.g.
   `dataZone`, `typeOfAssessment`), 7 only for England & Wales (e.g. `localAuthority`,
   `wallsEnergyEff`). Some of these are parallel "twin" columns for the same concept
   under different names (`wallsEnergyEff` vs `wallEnergyEff`, `heatLossCorridor` vs
   `heatLossCorridoor` — the double-`o` is a genuine typo in the source SERL data).
2. **Shared columns, different value vocabulary** — a handful of "All"-region columns
   use a different value format per nation for the same underlying concept
   (`glazedArea`, `constructionAgeBand`, and the multi-element `|` convention on
   `floorEnergyEff`/`roofEnergyEff`).
3. **Shared columns, same vocabulary** — the majority of columns (numeric costs,
   ratings, descriptions, etc.) behave identically regardless of nation.

The mock generator currently only implements pattern 3 fully, plus 4 of the pattern-2
fields (`glazedArea`, `constructionAgeBand`, `floorEnergyEff`, `roofEnergyEff`). The 22
Scotland-only columns are documented in the dictionary but not yet produced by
`generate_epc()` — see the **Generated** column below.

## Legend

- **England & Wales** / **Scotland** — `Yes`/`No`: whether the variable is populated
  (non-null) for records of that nation in the real dataset.
- **Generated** — `Yes`/`No`: whether this variable is included in the mock data
  generator (`_epc_fields()` in `generator_contextual_data.py`).
- **Notes** — special cases: different value vocab by region, region-exclusive twin
  columns, known generator/dictionary gaps, and fields that are only populated under
  specific conditions (e.g. `airTightnessEnergyEff` is Scotland-only and only populated
  for SAP assessments of new-build properties, not RdSAP assessments of existing
  dwellings). Fill-rate / share-of-PUPRN statistics are intentionally omitted here
  pending a check against the actual SERL data.

## Table

| Variable | England & Wales | Scotland | Generated | Notes |
|---|---|---|---|---|
| `3YrEnergyCostCurrent` | No | Yes | No | Scotland-only field (no England & Wales equivalent column). *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `3YrEnergySavingPotential` | No | Yes | No | Scotland-only field (no England & Wales equivalent column). *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `airTightnessDescription` | No | Yes | No | Scotland-only; only populated for SAP assessments (new-build), not RdSAP (existing dwellings). *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `airTightnessEnergyEff` | No | Yes | No | Scotland-only; same SAP-only availability pattern as `airTightnessDescription`. *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `airTightnessEnvEff` | No | Yes | No | Scotland-only; same SAP-only availability pattern as `airTightnessDescription`. *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `alternativeImprovements` | No | Yes | No | Scotland-only field (no England & Wales equivalent column). *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `builtForm` | Yes | Yes | Yes | Same values in both nations. |
| `co2EmissCurrPerFloorArea` | Yes | Yes | Yes | Same values in both nations. |
| `co2EmissionsCurrent` | Yes | Yes | Yes | Same values in both nations. |
| `co2EmissionsPotential` | Yes | Yes | Yes | Same values in both nations. |
| `constituency` | Yes | Yes | Yes | Same values in both nations. |
| `constructionAgeBand` | Yes | Yes | Yes | Same concept, different decade boundaries by nation — e.g. England & Wales has `1950-1966`, `1976-1982`, `1996-2002`; Scotland has `1950-1964`, `1976-1983`, `1999-2002`. `1930-1949` and `2012 onwards` are identical text in both nations and marked All. **Edition08 deviation:** in the real edition08 data (unlike this dictionary's edition07 values, and unlike the mock pipeline's pre-edition08 behavior), England & Wales values carry an `"England and Wales: "` prefix (e.g. `England and Wales: 1950-1966`); Scotland's values are unprefixed. Either nation can also be `NULL`, `INVALID`, or `NO DATA!` instead of a band. The mock generator implements the edition08 format. |
| `currentEnergyEfficiency` | Yes | Yes | Yes | Same values in both nations. |
| `currentEnergyRating` | Yes | Yes | Yes | Same values in both nations. |
| `currentEnvironmentalRating` | No | Yes | No | Scotland-only. *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `dataZone` | No | Yes | No | Scotland-only field (no England & Wales equivalent column). *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `energyConsumptionCurrent` | Yes | Yes | Yes | Same values in both nations. |
| `energyConsumptionPotential` | Yes | Yes | Yes | Same values in both nations. |
| `energyTariff` | Yes | Yes | Yes | Same values in both nations. |
| `environmentImpactCurrent` | Yes | Yes | Yes | Same values in both nations. |
| `environmentImpactPotential` | Yes | Yes | Yes | Same values in both nations. |
| `epcVersion` | Yes | Yes | Yes | Same values in both nations. |
| `extensionCount` | Yes | Yes | Yes | Same values in both nations. |
| `fixedLightingOutletsCount` | Yes | Yes | Yes | Same values in both nations. |
| `flatStoreyCount` | Yes | No | Yes | England & Wales-only field (no Scotland equivalent column). |
| `flatTopStorey` | Yes | Yes | Yes | Same values in both nations. |
| `floorDescription` | Yes | Yes | Yes | Same values in both nations. |
| `floorEnergyEff` | Yes | Yes | Yes | Single rating (`Average`…`Very Poor`, or `N/A`) for both nations, but Scotland may report multiple floor elements pipe-joined (`N/A \| N/A`, `N/A \| N/A \| N/A`); England & Wales never reports more than one element per property. See SERL EPC documentation edition07, p.1. |
| `floorEnvEff` | Yes | Yes | Yes | Known inconsistency: the dictionary still lists `N/A \| N/A` as `All` rather than `Scotland` (unlike `floorEnergyEff`, which was corrected). The generator does not currently produce `N/A` or multi-element values for this field at all — it falls back to the generic single-rating pool. Flagged for follow-up. |
| `floorHeight` | Yes | Yes | Yes | Same values in both nations. |
| `floorLevel` | Yes | Yes | Yes | Same values in both nations. |
| `glazedArea` | Yes | Yes | Yes | Different value vocabulary for the same concept. England & Wales use text labels (`Normal`, `Less Than Typical`, `More Than Typical`, `Much Less/More Than Typical`). Scotland uses a numeric band code with different semantics (`0`=New dwelling, `1`=Typical, `2`=More than typical, `4`=Much more than typical, `5`=Much less than typical). `NO DATA!` and blank occur in both. |
| `glazedType` | Yes | Yes | Yes | Same values in both nations. |
| `heatingCostCurrent` | Yes | Yes | Yes | Same values in both nations. |
| `heatingCostPotential` | Yes | Yes | Yes | Same values in both nations. |
| `heatLossCorridoor` | No | Yes | No | Scotland-only twin of `heatLossCorridor` (England & Wales). The double-`o` is a genuine typo in the source SERL data, not an error in this dictionary. *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `heatLossCorridor` | Yes | No | Yes | England & Wales-only twin of Scotland's `heatLossCorridoor` (same concept, different column — the extra `o` in the Scotland field name is a genuine typo carried over from the source SERL data). Only applies to flats/maisonettes. |
| `hotWaterCostCurrent` | Yes | Yes | Yes | Same values in both nations. |
| `hotWaterCostPotential` | Yes | Yes | Yes | Same values in both nations. |
| `hotwaterDescription` | Yes | Yes | Yes | Same values in both nations. |
| `hotWaterEnergyEff` | Yes | Yes | Yes | Per SERL documentation the Scotland multi-element `\|` convention applies to all "element performance summary" fields, but only `floorEnergyEff`/`roofEnergyEff` have been enumerated with combos so far — this field is currently generated as a single rating only for both nations (known simplification, not a confirmed real-data gap). |
| `hotWaterEnvEff` | Yes | Yes | Yes | Per SERL documentation the Scotland multi-element `\|` convention applies to all "element performance summary" fields, but only `floorEnergyEff`/`roofEnergyEff` have been enumerated with combos so far — this field is currently generated as a single rating only for both nations (known simplification, not a confirmed real-data gap). |
| `impactCavityWallInsulation` | No | Yes | No | Scotland-only field (no England & Wales equivalent column). *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `impactLoftInsulation` | No | Yes | No | Scotland-only field (no England & Wales equivalent column). *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `impactSolidWallInsulation` | No | Yes | No | Scotland-only field (no England & Wales equivalent column). *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `improvements` | No | Yes | No | Scotland-only field (no England & Wales equivalent column). *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `inspectionDate` | Yes | Yes | Yes | Same values in both nations. |
| `lightingCostCurrent` | Yes | Yes | Yes | Same values in both nations. |
| `lightingCostPotential` | Yes | Yes | Yes | Same values in both nations. |
| `lightingDescription` | Yes | Yes | Yes | Same values in both nations. |
| `lightingEnergyEff` | Yes | Yes | Yes | Per SERL documentation the Scotland multi-element `\|` convention applies to all "element performance summary" fields, but only `floorEnergyEff`/`roofEnergyEff` have been enumerated with combos so far — this field is currently generated as a single rating only for both nations (known simplification, not a confirmed real-data gap). |
| `lightingEnvEff` | Yes | Yes | Yes | Per SERL documentation the Scotland multi-element `\|` convention applies to all "element performance summary" fields, but only `floorEnergyEff`/`roofEnergyEff` have been enumerated with combos so far — this field is currently generated as a single rating only for both nations (known simplification, not a confirmed real-data gap). |
| `localAuthority` | Yes | No | Yes | England & Wales-only field (no Scotland equivalent column). |
| `lodgementDate` | Yes | Yes | Yes | Same values in both nations. |
| `lodgementDatetime` | Yes | No | Yes | England & Wales-only field (no Scotland equivalent column). |
| `lowEnergyFixedLightCount` | Yes | Yes | Yes | Same values in both nations. |
| `lowEnergyLighting` | Yes | Yes | Yes | Same values in both nations. |
| `lzcEnergySources` | No | Yes | No | Scotland-only field (no England & Wales equivalent column). *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `mainFuel` | Yes | Yes | Yes | Same values in both nations. |
| `mainheatcEnergyEff` | Yes | Yes | Yes | Per SERL documentation the Scotland multi-element `\|` convention applies to all "element performance summary" fields, but only `floorEnergyEff`/`roofEnergyEff` have been enumerated with combos so far — this field is currently generated as a single rating only for both nations (known simplification, not a confirmed real-data gap). |
| `mainheatcEnvEff` | Yes | Yes | Yes | Per SERL documentation the Scotland multi-element `\|` convention applies to all "element performance summary" fields, but only `floorEnergyEff`/`roofEnergyEff` have been enumerated with combos so far — this field is currently generated as a single rating only for both nations (known simplification, not a confirmed real-data gap). |
| `mainheatcontDescription` | Yes | Yes | Yes | Same values in both nations. |
| `mainheatDescription` | Yes | Yes | Yes | Same values in both nations. |
| `mainheatEnergyEff` | Yes | Yes | Yes | Per SERL documentation the Scotland multi-element `\|` convention applies to all "element performance summary" fields, but only `floorEnergyEff`/`roofEnergyEff` have been enumerated with combos so far — this field is currently generated as a single rating only for both nations (known simplification, not a confirmed real-data gap). |
| `mainheatEnvEff` | Yes | Yes | Yes | Per SERL documentation the Scotland multi-element `\|` convention applies to all "element performance summary" fields, but only `floorEnergyEff`/`roofEnergyEff` have been enumerated with combos so far — this field is currently generated as a single rating only for both nations (known simplification, not a confirmed real-data gap). |
| `mainHeatingCategory` | No | Yes | No | Scotland-only. *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `mainHeatingControls` | Yes | Yes | Yes | Same values in both nations. |
| `mainsGasFlag` | Yes | Yes | Yes | Same values in both nations. |
| `mechanicalVentilation` | Yes | Yes | Yes | Same values in both nations. |
| `multiGlazeProportion` | Yes | Yes | Yes | Same values in both nations. |
| `numberHabitableRooms` | Yes | Yes | Yes | Same values in both nations. |
| `numberHeatedRooms` | Yes | Yes | Yes | Same values in both nations. |
| `numberOpenFireplaces` | Yes | Yes | Yes | Same values in both nations. |
| `photoSupply` | Yes | Yes | Yes | Same values in both nations. |
| `potentialEnergyEfficiency` | Yes | Yes | Yes | Same values in both nations. |
| `potentialEnergyRating` | Yes | Yes | Yes | Same values in both nations. |
| `potentialEnvironmentalRating` | No | Yes | No | Scotland-only, parallel to `currentEnvironmentalRating`. *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `propertyType` | Yes | Yes | Yes | Same values in both nations. |
| `PUPRN` | Yes | Yes | Yes | Same values in both nations. |
| `roofDescription` | Yes | Yes | Yes | Same values in both nations. |
| `roofEnergyEff` | Yes | Yes | Yes | Same single-vs-multi-element rule as `floorEnergyEff`. England & Wales is always a single rating (`Average`…`Very Poor`). Scotland may report 2 or 3 roof elements pipe-joined (e.g. `Average \| Good`, `Average \| Good \| Poor`), including the mixed case `N/A \| Very Poor`. |
| `roofEnvEff` | Yes | Yes | Yes | Per the general Scotland multi-element rule this field should behave like `roofEnergyEff`, but only single ratings are documented and generated (generic 5-rating pool) — combos not yet implemented. Flagged for follow-up. |
| `secondheatDescription` | Yes | Yes | Yes | Same values in both nations. |
| `sheatingEnergyEff` | Yes | Yes | Yes | Dictionary only documents blank or `N/A` (most dwellings have no secondary heating system). The generator's generic single-rating branch can still emit `Average/Good/Poor/Very Good/Very Poor` for this field, which falls outside the documented value set. Flagged for follow-up. |
| `sheatingEnvEff` | Yes | Yes | Yes | Same pattern as `sheatingEnergyEff` — dictionary only documents blank/`N/A`; generator can emit values outside that set, flagged for follow-up. |
| `solarWaterHeatingFlag` | Yes | Yes | Yes | Same values in both nations. |
| `spaceHeatingDemand` | No | Yes | No | Scotland-only field (no England & Wales equivalent column). *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `tenure` | Yes | Yes | Yes | Same values in both nations. |
| `totalFloorArea` | Yes | Yes | Yes | Same values in both nations. |
| `transactionType` | Yes | Yes | Yes | Same values in both nations. |
| `typeOfAssessment` | No | Yes | No | Scotland-only; the only two documented values are blank and `RdSAP, existing dwelling`. *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `unheatedCorridorLength` | Yes | Yes | Yes | Same values in both nations. |
| `wallDescription` | No | Yes | No | Scotland-only twin of `wallsDescription` (England & Wales, plural). *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `wallEnergyEff` | No | Yes | No | Scotland-only twin of `wallsEnergyEff` (England & Wales, plural). Follows the Scotland multi-element `\|` convention (real edition07 example: `Poor \| Good`) but, unlike `roofEnergyEff`, combos are not yet enumerated as explicit rows in this dictionary — only a blank placeholder is documented. *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `wallEnvEff` | No | Yes | No | Scotland-only twin of `wallsEnvEff` (England & Wales, plural). Same multi-element `\|` convention and same not-yet-enumerated gap as `wallEnergyEff`. *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `wallsDescription` | Yes | No | Yes | England & Wales-only twin of Scotland's `wallDescription` (singular, no `s`). Same concept, parallel columns per nation. |
| `wallsEnergyEff` | Yes | No | Yes | England & Wales-only twin of Scotland's `wallEnergyEff` (singular, no `s`). |
| `wallsEnvEff` | Yes | No | Yes | England & Wales-only twin of Scotland's `wallEnvEff` (singular, no `s`). |
| `waterHeatingDemand` | No | Yes | No | Scotland-only field (no England & Wales equivalent column). *Not yet generated by the mock pipeline (absent from `_epc_fields()`); documented here from the real edition07 schema only.* |
| `windowsDescription` | Yes | Yes | Yes | Same values in both nations. |
| `windowsEnergyEff` | Yes | Yes | Yes | Per SERL documentation the Scotland multi-element `\|` convention applies to all "element performance summary" fields, but only `floorEnergyEff`/`roofEnergyEff` have been enumerated with combos so far — this field is currently generated as a single rating only for both nations (known simplification, not a confirmed real-data gap). |
| `windowsEnvEff` | Yes | Yes | Yes | Per SERL documentation the Scotland multi-element `\|` convention applies to all "element performance summary" fields, but only `floorEnergyEff`/`roofEnergyEff` have been enumerated with combos so far — this field is currently generated as a single rating only for both nations (known simplification, not a confirmed real-data gap). |
| `windTurbineCount` | Yes | Yes | Yes | Same values in both nations. |

## Sources

- `docs/documentation/SERL/8666_serl_epc_documentation_edition07.pdf` — SERL EPC technical
  documentation (real edition07 field list, unique-value counts, and value-frequency
  tables used for the "mostly null" and "well-populated" notes above).
- `docs/documentation/EPC/EPC_data_dictionary_England_Wales.ods` — official DLUHC EPC
  register glossary (England & Wales field definitions).
- `docs/documentation/EPC/EPC_data_dictionary_Scotland.pdf` — official Scottish EPC
  register data dictionary (Scotland field definitions, including the numeric
  `glazedArea` codes and Scotland's `constructionAgeBand` boundaries).
- `data/reference/edition08/serl_epc_data_dictionary_edition07.csv` — this repo's per-value
  dictionary, the source of truth cross-checked against the mock generator's output
  (see `src/serl_mock/generator_contextual_data.py::generate_epc`).